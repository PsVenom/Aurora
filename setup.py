from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A novel text-to-image generation paradigm using CLIP and StyleGAN to generate images. The model uses  unique training paradigm for latent vector guidance',
    author='PsVenom',
    license='MIT',
)
